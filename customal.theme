<?php

/**
 * Implements hook_preprocess_HOOK() for HTML document templates.
 * @param $variables
 */
function customal_preprocess_html(&$variables)
{
    // Add our own customal class to the body.
    $variables['attributes']['class'][] = 'customal';
}
